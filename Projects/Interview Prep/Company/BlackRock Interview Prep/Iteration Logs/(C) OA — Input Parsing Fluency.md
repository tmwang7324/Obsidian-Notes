---
type: next-step
project: BlackRock Interview Prep
status: done
goal: "[[(C) Land BlackRock Aladdin Offer]]"
effort: S
tags: [next-step, blackrock-prep]
updated: 2026-08-08
---

# (C) OA — Input Parsing Fluency

Drill the canonical HackerRank stdin formats until parsing is muscle memory, so 100%
of attention on the assessment goes to the algorithm, not the harness.

> **Resolved 2026-08-08 — closed with the pipeline.** The BlackRock loop went silent after
> the ~2026-07-11 OA deadline (see [[(C) Land BlackRock Aladdin Offer]], `status: stalled`).
> **This skill is not BlackRock-specific** — self-parsed stdin shows up in most OAs, so if
> it's still shaky, re-open it as a shared step under `Interview Prep/Leetcode/`, not here.

**Formats to be able to type cold (Python):**
- Single int / single line of space-separated ints
- `N` then `N` lines (list of values)
- `N` then a grid / matrix
- `T` test cases, each a block
- Mixed types on one line

**Done when:** I can parse any of the above without thinking and print output in the
expected format. Confirm the harness mode first (pre-written stub vs. raw stdin).
