---
type: progress
project: Doculyze
date: 2026-07-08
summary: Mapped current-state auth architecture + doc-vs-code drift
goals: ["[[Doculyze MVP]]"]
tags: [progress, doculyze]
sources: 3
---

# (C) 2026-07-08 — Mapped the real auth architecture and its drift from the plan

**Advances:** [[Doculyze MVP]]

## Done
- Wrote a full **current-state architecture map** (auth phase) documenting how auth *actually exists in code*: re-implemented inside the Next.js app as **Server Actions** using the **Firebase Admin SDK**, not the Express `backend/` the docs describe.
- Documented the **two-Firebase-SDK split** (client SDK for credentials/ID token → Server Action mints an httpOnly session cookie) and the live `refresh`-cookie session model (with `access` cookie defined-but-unwired).
- Catalogued **legacy / dead code** (Express `backend/`, `api_with_express.tsx`, `actions.tsx`, `auth_check.tsx`, stray Pages-router file, empty `database.tsx`) and flagged the doc-vs-code drift in `CLAUDE.md`/`PLAN.md`.
- Flagged init smells (committed `firebaseConfig` + `serviceAccountKey.json`; env-based service account built then ignored) and the stubbed document-upload seam (hardcoded Firestore write).
- Started supporting notes: a **Current Architecture** service/dataflow stub and an **HTML/Forms** learning note.

## Next steps
- [[(C) Retire or Revive the Legacy Express Backend|Retire or Revive the Legacy Express Backend]] — decide per dead-code item (delete vs. revive), then reconcile `CLAUDE.md`/`PLAN.md` to the Server-Action reality.
- [[(C) Wire Document Upload to Firebase Storage|Wire Document Upload to Firebase Storage]] — replace the stubbed placeholder write at the upload seam.

## Sources
- [[DocuLyze-Architecture]] — full current-state auth map + drift/legacy analysis
- [[Current Architecture]] — service/dataflow stub
- [[Forms]] — HTML forms learning note
