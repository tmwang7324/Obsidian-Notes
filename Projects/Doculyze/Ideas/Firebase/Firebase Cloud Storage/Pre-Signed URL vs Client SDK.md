# Pre-Signed URLs (Server-to-Client Approach)
A pre-signed URL is a time-limited, cryptographic link generated by my backend server. It gies a client temporary permission to upload or download a specific file directly to the cloud bucket without needing Firebase infrastructure.

```typescript
import { getAuth } from ''
export async function getPresignedUrl(filePath: string, fileType: string)
{
	const 
	const bucket = storage.bucket(FIREBASE_STORAGE_)
}

```