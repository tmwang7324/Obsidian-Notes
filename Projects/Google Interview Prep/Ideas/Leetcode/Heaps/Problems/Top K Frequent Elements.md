# Approach
Use `Counter` class to count the frequencies of each element in given array.
Call the `most_common()` method within the `Counter` class, which returns a list of tuples containing the item and its count sorted in descending order.
Return only the first element in each tuple in an array. 

```python

from collections import Counter
class Solution:
	def topK(self, nums: List[int]) -> List[int]:
	
	

```
