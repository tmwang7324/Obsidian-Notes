---
type: plan
project: Google Interview Prep
date: 2026-07-11
intensity: high
hours: 3
tags: [plan, google-prep, doculyze]
---

# (C) 2026-07-11 — DP Rung 2 under the clock + finally close file upload

**Focus:** [[(C) Google SWE Offer Roadmap|Google Interview Prep]] · **Budget:** ~3h (3h × high) · DP owns the day; Doculyze is capped evening margin.

> **The 25-minute rule (the real point of today).** If you don't have a *working recursion* — state, transition, base case — written in 25 min, you STOP. Read the editorial's **state definition only** (not the code), then re-implement. Pivoting on time is the skill; grinding is the anti-skill. An hour on an easy problem is a no-hire even when correct.

## Today's goals
**Advances:** [[(C) Google SWE Offer Roadmap]]
- [ ] **Re-derive Coin Change cold** — brute-force recursion first (state = amount remaining), *then* add memo, *then* convert to bottom-up table. Narrate aloud. 25-min pivot rule armed. (M)
- [ ] **See the delta** — adapt that same table to Coin Change II (min → *count ways*) and Word Break (reachability over string indices). For each, say aloud *the one line that changes.* That "what changed" reflex **is** DP recognition. (M)

## Doculyze — evening margin, capped 1h, DP-first (cut if DP runs over)
- [x] Finish file upload: create Storage bucket + set `FIREBASE_STORAGE_BUCKET`, then wire bytes in `upload_document.tsx` (mint doc-ref id → `storage.bucket().file(...).save(buffer)` → `createDocumentRecord`). Fully spec'd — this is the *downshift* task, not a thinking task. — [[(C) Wire Document Upload to Firebase Storage]] (M)

## Non-negotiables
- [x] 🙏 Faith / prayer

_Reminders: if any time is left, grade the Jul 2 graphs master-check (still ungraded — graphs aren't formally certified) · move · protect rest._
