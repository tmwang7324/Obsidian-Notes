# Insert


# Read

# Update

# Delete

# Upsert
An **upsert** (a portamnteau of **up**date and in**sert**) is a database operation that automatically modifieds an existing record if it already exists, or creates a new record if it does not.