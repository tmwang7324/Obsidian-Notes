# Overview
Dynamically rendered pages call `header()` aspects such as `cookies()` while statically rendered pages do not.
Statically rendered pages are usually hosted through the CDN (content delivery networks) for faster speed.